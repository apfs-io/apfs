#!/usr/bin/env bash

# Two-file convert (not mogrify): plugeproc pre-creates $2 as an empty
# extensionless temp. mogrify treats every leftover arg as an in-place image
# and fails with "no decode delegate for this image format ''".
convert "$1" -strip -interlace Plane -auto-orient "$2"
